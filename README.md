# homebrew-tap

Homebrew tap for [hscli](https://github.com/wavyx/hscli) — the Help Scout CLI.

```bash
brew tap wavyx/tap
brew install hscli
```

The formula is generated from the published npm tarball by
[`scripts/gen-dist.mjs`](https://github.com/wavyx/hscli/blob/main/scripts/gen-dist.mjs).
